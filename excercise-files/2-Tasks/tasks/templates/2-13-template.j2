We are in group: {% for group in hostvars[inventory_hostname]['group_names'] %}{{group}}{% if not loop.last %}, {% endif %}{% endfor %}

We like both {% for key,value in animals.items() %}{{key}}{% if not loop.last %}, {% endif %}{% endfor %}

We{% for key,value in animals.items() %} like{% for animal,name in animals[key].items() %} {{name.color}} {{animal}}s{% if not loop.last %} and{% endif %}{% endfor %}{% if not loop.last %} and{% endif %}{% endfor %}
